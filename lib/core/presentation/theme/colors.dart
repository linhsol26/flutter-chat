import 'dart:ui';

import 'package:material_color_generator/material_color_generator.dart';

const primaryColor = Color(0xFF95BCCC);
final primaryColorMaterial = generateMaterialColor(color: primaryColor);
const subColor = Color.fromARGB(255, 182, 212, 224);
final subColorMaterial = generateMaterialColor(color: subColor);
const accentColor = Color.fromARGB(255, 250, 232, 232);
final accentColorMaterial = generateMaterialColor(color: accentColor);
const backgroundDarkColor = Color.fromARGB(255, 70, 104, 118);
final backgroundDarkColorMaterial = generateMaterialColor(color: backgroundDarkColor);
const backgroundLightColor = Color.fromARGB(255, 219, 234, 246);
final backgroundLightColorMaterial = generateMaterialColor(color: backgroundLightColor);
const blackColor = Color(0xFF000E08);
final blackColorMaterial = generateMaterialColor(color: blackColor);
const blackSubColor = Color(0xFF181818);
final blackSubColorMaterial = generateMaterialColor(color: blackColor);
const textColor = Color(0xFFFFFFFF);
final textColorMaterial = generateMaterialColor(color: textColor);
const whiteColor = Color(0xFFFFFFFF);
final whiteColorMaterial = generateMaterialColor(color: whiteColor);
const redColor = Color(0xFFFF2D1B);
final redColorMaterial = generateMaterialColor(color: redColor);
const greyColor = Color(0xFFF3F6F6);
final greyColorMaterial = generateMaterialColor(color: greyColor);
const brownColor = Color(0xFF988080);
final brownColorMaterial = generateMaterialColor(color: brownColor);
